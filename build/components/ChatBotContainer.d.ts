import React, { ReactNode } from "react";
declare const ChatBotMainContainer: ({ children }: Props) => React.JSX.Element;
type Props = {
    children: ReactNode;
};
export default ChatBotMainContainer;
