import React from "react";
import { ActionProviderProps } from "../types/rckTypes";
declare const ActionProvider: ({ children, createChatBotMessage, setState, state, }: ActionProviderProps) => React.JSX.Element;
export default ActionProvider;
